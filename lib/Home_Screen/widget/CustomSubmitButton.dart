import 'package:flutter/material.dart';

class dsSummit extends StatelessWidget {
  const dsSummit({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () {},
      child: Container(),
    );
  }
}
